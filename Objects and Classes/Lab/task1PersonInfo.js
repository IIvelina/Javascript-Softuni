function solve(firstName, lastName, age){

    age = Number(age);

    let personInfo = {firstName: firstName, lastName: lastName, age: age};

   return(personInfo);

}

solve("Peter", "Pan", "20");